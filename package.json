{
  "name": "voice-enable-worklisting",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix server run build && npm --prefix web run build",
    "start": "npm --prefix server start",
    "format": "npm --prefix server run format && npm --prefix web run format"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
